import axios from 'axios';
import { API } from '../utils/config';

export const productReview = (data: any) => {
    // console.log(data)
    // return axios.get(`${API}/review/admin/${data.index}`, data, {
    //     headers: {
    //         'Content-Type': 'application/json'
    //     }
    // })
}